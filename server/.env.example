# MySQL veritabanı bağlantısı
# Format: mysql://KULLANICI:SIFRE@HOST:PORT/VERITABANI
#
# MAMP MySQL örneği:
#   - Kullanıcı: root
#   - Şifre: root veya boş (MAMP varsayılanı genelde boş)
#   - Host: localhost
#   - Port: 3306
#   - Veritabanı: escafood_webkasa
#
DATABASE_URL="mysql://root:root@localhost:3306/escafood_webkasa"

# API sunucu portu (opsiyonel, varsayılan: 4000)
PORT=4000

# JWT token imzalama anahtarı (login için zorunlu)
JWT_SECRET="your-secure-random-secret-at-least-32-chars"
